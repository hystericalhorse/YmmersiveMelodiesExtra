$C = "../../Common.ui";

$C.@PageOverlay {}

$C.@Container {
    Anchor: (Width: 600, Height: 260);

    #Title {
        $C.@Title {
           @Text = %server.customUI.melodyUpload.title;
        }
    }

    #Content {
        LayoutMode: Top;

        Label #Details {
            Text: %server.customUI.melodyUpload.details;
            Padding: 6;
        }

        $C.@TextField #Name {
            @Anchor = (Left: 0, Top: 12);

            PlaceholderText: %server.customUI.melodyUpload.name;
        }

        $C.@TextField #Url {
            @Anchor = (Left: 0, Top: 12);

            PlaceholderText: %server.customUI.melodyUpload.url;
        }

        Group {
            LayoutMode: Left;
            Anchor: (Top: 15);

            $C.@SecondaryTextButton #Cancel {
                Text: %server.customUI.melodySelection.cancel;
                FlexWeight: 1;
            }

            Group {
                Anchor: (Width: 5);
            }

            $C.@TextButton #Upload {
                Text: %server.customUI.melodySelection.upload;
                FlexWeight: 1;
            }
        }
    }
}

$C.@BackButton {}
