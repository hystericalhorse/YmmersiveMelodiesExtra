$C = "../../Common.ui";

$C.@PageOverlay {}

$C.@Container {
    Anchor: (Width: 500, Height: 700);

    #Title {
        Group {
            $C.@Title {
               @Text = %server.customUI.melodySelection.title;
            }
        }
    }

    #Content {
        LayoutMode: Top;

        $C.@TextField #SearchInput {
            @Anchor = (Left: 4, Bottom: 4);

            PlaceholderText: %server.customUI.melodySelection.search;
            AutoFocus: true;
        }

        Group #MelodyList {
            FlexWeight: 1;
            LayoutMode: TopScrolling;
            ScrollbarStyle: $C.@DefaultScrollbarStyle;
        }

        Group {
            LayoutMode: Left;
            Anchor: (Top: 15);

            $C.@TextButton #Stop {
                Text: %server.customUI.melodySelection.stop;
                FlexWeight: 1;
            }

            Group {
                Anchor: (Width: 5);
            }

            $C.@TextButton #Delete {
                Text: %server.customUI.melodySelection.delete;
                FlexWeight: 1;
            }

            Group {
                Anchor: (Width: 5);
            }

            $C.@TextButton #Upload {
                Text: %server.customUI.melodySelection.upload;
                FlexWeight: 1;
            }
        }
    }
}

$C.@BackButton {}
